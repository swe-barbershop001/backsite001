# ==========================================
# Barbershop Bot - Environment Configuration
# ==========================================

# ==========================================
# Telegram Bot Configuration
# ==========================================
# Bot token from @BotFather
BOT_TOKEN=your_bot_token_here

# ==========================================
# Database Configuration
# ==========================================
# Database host (use 'db' for Docker, 'localhost' for local development)
DB_HOST=localhost
# Database port
DB_PORT=5432
# Database username
DB_USERNAME=postgres
# Database password
DB_PASSWORD=your_secure_password
# Database name
DB_DATABASE=barbershop_db

# Database synchronize (auto-create tables)
# WARNING: Set to 'false' in production! Use migrations instead.
# Development: true (quick development)
# Production: false (use migrations for safety)
DB_SYNCHRONIZE=true

# ==========================================
# Application Configuration
# ==========================================
# Application port
PORT=3000

# Node environment (development, production, test)
NODE_ENV=development

# ==========================================
# JWT Configuration
# ==========================================
# JWT secret key (use a strong random string in production)
JWT_TOKEN_SECRET=your_jwt_secret_key_here_min_32_characters

# JWT token expiration (e.g., 7d, 24h, 60m)
JWT_TOKEN_EXPIRATION=7d

# ==========================================
# Super Admin Configuration
# ==========================================
# Super admin credentials (created automatically on first run)
SUPER_ADMIN_USERNAME=super_admin
SUPER_ADMIN_PASSWORD=super_admin123
SUPER_ADMIN_NAME=Super Admin
SUPER_ADMIN_PHONE=+998900000000

# ==========================================
# Production Notes
# ==========================================
# For production deployment:
# 1. Set NODE_ENV=production
# 2. Set DB_SYNCHRONIZE=false
# 3. Use strong passwords for DB_PASSWORD and SUPER_ADMIN_PASSWORD
# 4. Use a secure random string (min 32 chars) for JWT_TOKEN_SECRET
# 5. Set DB_HOST=db when using Docker Compose
# 6. Ensure all sensitive values are properly secured
